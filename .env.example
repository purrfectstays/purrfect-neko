# Supabase Configuration (Required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# Note: Never expose service role key in client-side code
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Resend Configuration (Required for email functionality)
RESEND_API_KEY=your_resend_api_key

# App Configuration (Required)
VITE_APP_URL=https://your-app.vercel.app  # Update to your Vercel domain

# Analytics Configuration (Optional but recommended)
VITE_GA_MEASUREMENT_ID=your_ga_measurement_id

# Environment (Required)
NODE_ENV=production  # Set to 'development' for local development

# Email Domain Configuration (Required for Supabase Edge Functions)
SITE_URL=https://your-app.vercel.app  # Must match your Vercel domain

# Security Best Practices:
# 1. Never commit real API keys to version control
# 2. Use different keys for development and production
# 3. Regularly rotate API keys for security
# 4. Store production keys in a secure secret manager
# 5. Use environment-specific .env files (.env.development, .env.production)
# 6. Add .env* to your .gitignore file
# 7. Set appropriate CORS and security headers in production
# 8. Enable rate limiting on authentication endpoints
# 9. Use HTTPS in production
# 10. Keep dependencies updated and audit regularly